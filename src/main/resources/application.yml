spring:
  application:
    name: intranet-server

  datasource:
    url: jdbc:mysql://localhost:3307/intranet?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: gktjdwns1030!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 100MB

jwt:
  secret: dcom-intranet-server-jwt-secret-key-must-be-long-enough-for-256-bits-security
  access-token-validity: 1800000
  refresh-token-validity: 1209600000

file:
  upload-dir: ./uploads/info